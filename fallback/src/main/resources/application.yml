server:
  port: 80
spring:
  application:
    name: fallback
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:7001/eureka/
    register-with-eureka: false
feign:
  hystrix:
    enabled: true
